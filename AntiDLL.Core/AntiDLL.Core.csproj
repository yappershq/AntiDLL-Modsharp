<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <AssemblyName>AntiDLL</AssemblyName>
    <RootNamespace>AntiDLL</RootNamespace>
    <!-- Native mid-hook on ListenBitsReceived reads the per-client proxy via raw pointers. -->
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
  </PropertyGroup>

</Project>
